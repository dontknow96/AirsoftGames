import 'package:flutter/cupertino.dart';

class FlagpostDeviceView extends StatelessWidget{
  const FlagpostDeviceView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('shizzle');
  }
}
